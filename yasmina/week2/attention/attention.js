// alert ("js is working")
 
var stockprice = parseInt(prompt("How much is the ADDA stock price?"));

while (stockprice >= 15){

	parseInt(prompt( "What is the price of ADDA stock now?"));
}
if (stockprice <15) {

	alert("Sell all your stock!");
}
